//SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract ArraySum{
    uint[] public s;

    function sum(uint n)public{
        for (uint i=0; n>=i; i++){
            uint add;
        s.push(add+=i);
        }
    }
}